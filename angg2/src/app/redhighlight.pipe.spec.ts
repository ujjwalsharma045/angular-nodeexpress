import { RedhighlightPipe } from './redhighlight.pipe';

describe('RedhighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new RedhighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
