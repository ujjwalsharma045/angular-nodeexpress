import { Directive7Directive } from './directive7.directive';

describe('Directive7Directive', () => {
  it('should create an instance', () => {
    const directive = new Directive7Directive();
    expect(directive).toBeTruthy();
  });
});
