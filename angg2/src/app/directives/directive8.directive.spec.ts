import { Directive8Directive } from './directive8.directive';

describe('Directive8Directive', () => {
  it('should create an instance', () => {
    const directive = new Directive8Directive();
    expect(directive).toBeTruthy();
  });
});
