import { StatusformatPipe } from './statusformat.pipe';

describe('StatusformatPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusformatPipe();
    expect(pipe).toBeTruthy();
  });
});
