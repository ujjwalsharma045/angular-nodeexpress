import { InvalidcharacterDirective } from './invalidcharacter.directive';

describe('InvalidcharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidcharacterDirective();
    expect(directive).toBeTruthy();
  });
});
