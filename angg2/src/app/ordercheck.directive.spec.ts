import { OrdercheckDirective } from './ordercheck.directive';

describe('OrdercheckDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdercheckDirective();
    expect(directive).toBeTruthy();
  });
});
