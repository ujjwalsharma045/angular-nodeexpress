import { DatetimedifferPipe } from './datetimediffer.pipe';

describe('DatetimedifferPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimedifferPipe();
    expect(pipe).toBeTruthy();
  });
});
