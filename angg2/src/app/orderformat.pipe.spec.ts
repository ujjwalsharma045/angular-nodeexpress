import { OrderformatPipe } from './orderformat.pipe';

describe('OrderformatPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderformatPipe();
    expect(pipe).toBeTruthy();
  });
});
