import { CostPriceComparisionPipe } from './cost-price-comparision.pipe';

describe('CostPriceComparisionPipe', () => {
  it('create an instance', () => {
    const pipe = new CostPriceComparisionPipe();
    expect(pipe).toBeTruthy();
  });
});
